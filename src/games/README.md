# Mini-games

Each mini-game owns one folder containing its scene, entities, rules, level
configuration, and game-specific assets or loaders. A game must integrate with
the shared contract and registry that will be introduced in Phase 6.

The MVP's first game is `tiny-tank/`. Future game folders should be created only
when their implementation begins.
